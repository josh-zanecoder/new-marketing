import React from 'react';

import CompiledEmailPreview from './CompiledEmailPreview';

/** @deprecated Use CompiledEmailPreview — kept for import compatibility. */
export default function ImportedHtmlPreview() {
  return <CompiledEmailPreview title="Imported HTML preview" />;
}
